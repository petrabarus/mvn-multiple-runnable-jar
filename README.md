# mvn-multiple-runnable-jar

Test maven to create multiple runnable jar.

To build, execute

```
mvn install
```

The executables will be `target/test-multiple-jar-app1.jar` and `target/test-multiple-jar-app2.jar`.
